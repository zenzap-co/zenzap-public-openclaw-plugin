{
  "name": "@zenzap-co/openclaw-plugin",
  "version": "0.1.11",
  "description": "Zenzap channel plugin for OpenClaw — AI assistant in your team topics",
  "type": "module",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist/",
    "openclaw.plugin.json",
    "skills/"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "prebuild": "npm run clean",
    "bundle": "pnpm bundle:main && pnpm bundle:offset-store && pnpm bundle:session-cleanup",
    "bundle:main": "esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --external:./offset-store.js --external:./session-cleanup.js --external:crypto --external:path --external:fs --external:os --external:module",
    "bundle:offset-store": "esbuild src/offset-store.ts --bundle --platform=node --format=esm --outfile=dist/offset-store.js --external:crypto --external:path --external:fs --external:os --external:module",
    "bundle:session-cleanup": "esbuild src/session-cleanup.ts --bundle --platform=node --format=esm --outfile=dist/session-cleanup.js --external:crypto --external:path --external:fs --external:os --external:module",
    "prepack": "pnpm --filter @zenzap-co/sdk run build && pnpm run bundle && pnpm run build:types",
    "build:types": "tsc --emitDeclarationOnly",
    "deploy:build": "pnpm bundle",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=22"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zenzap-co/zenzap-public-openclaw-plugin.git",
    "directory": "packages/openclaw"
  },
  "devDependencies": {
    "@zenzap-co/sdk": "workspace:*",
    "@types/node": "^22.0.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ],
    "channel": {
      "id": "zenzap",
      "label": "Zenzap",
      "selectionLabel": "Zenzap (Team Chat)",
      "docsPath": "/channels/zenzap",
      "docsLabel": "zenzap",
      "blurb": "AI assistant in your Zenzap team topics.",
      "order": 90
    },
    "install": {
      "npmSpec": "@zenzap-co/openclaw-plugin",
      "defaultChoice": "npm"
    }
  }
}
